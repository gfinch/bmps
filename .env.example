# BMPS Core Application - Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# Application Configuration
# ============================================================================

# Port for the REST API server (default: 8081)
BMPS_PORT=8081

# Read-only mode: if true, runs automated trading for current day then exits
# if false, runs REST API server indefinitely for manual control
BMPS_READ_ONLY_MODE=false

# ============================================================================
# Data Source API Keys (REQUIRED)
# ============================================================================

# Databento API Key - Required for accessing market data
# Get your key from: https://databento.com/
DATABENTO_KEY=your_databento_api_key_here

# ============================================================================
# Tradovate Broker Configuration (Optional - only if using TradovateBroker)
# ============================================================================

# Tradovate credentials for live/demo trading
# Leave empty if only using SimulatedAccountBroker
# Note: All other Tradovate settings (username, client-id, account-id, etc.)
# are configured in core/src/main/resources/application.conf

# Tradovate password
TRADOVATE_PASS=

# Tradovate API secret key
TRADOVATE_KEY=

# Tradovate device ID (unique identifier for this instance)
TRADOVATE_DEVICE=

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
# 
# 1. NEVER commit the .env file to version control - it contains secrets!
# 2. Make sure .env is listed in .gitignore
# 3. Each user must have their own API keys and broker accounts
# 4. Read LEGAL_DISCLAIMER.md before using this software
# 5. The application.conf file in core/src/main/resources/ can be customized
#    for broker accounts, risk settings, and data source selection
